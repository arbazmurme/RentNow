<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/output.css">
</head>
<body class="bg-gray-100 font-sans">
    <div class="container mx-auto py-16 px-6">
        <h2 class="text-3xl font-extrabold text-gray-800 text-center mb-10">Edit House Details</h2>
        
        <form action="/edit-house/<%= house.id %>" method="POST" class="bg-white p-6 shadow-lg rounded-lg max-w-lg mx-auto">
            <div class="mb-4">
                <label for="name" class="block text-gray-700 font-bold">House Name:</label>
                <input type="text" id="name" name="name" value="<%= house.name %>" class="w-full p-2 border rounded-lg">
            </div>
            <div class="mb-4">
                <label for="address" class="block text-gray-700 font-bold">Address:</label>
                <input type="text" id="address" name="address" value="<%= house.address %>" class="w-full p-2 border rounded-lg">
            </div>
            <div class="mb-4">
                <label for="price" class="block text-gray-700 font-bold">Price (₹/month):</label>
                <input type="number" id="price" name="price" value="<%= house.price %>" class="w-full p-2 border rounded-lg">
            </div>
            <div class="flex justify-between">
                <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-500">Save Changes</button>
                <a href="/edit-house" class="px-4 py-2 bg-gray-500 text-white rounded-lg hover:bg-gray-400">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>
