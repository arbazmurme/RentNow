<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= house.name %> - Details</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">

    <!-- Header -->
    <header class="bg-blue-600 text-white p-6 text-center">
        <h1 class="text-3xl font-bold">Welcome to RentNow</h1>
        <p class="text-lg">Find your dream home with us</p>
    </header>

    <!-- Main Content -->
    <div class="container mx-auto p-8">
        <div class="bg-white shadow-lg rounded-lg overflow-hidden">
            <!-- House Image -->
            <img src="<%= house.image %>" alt="<%= house.name %>" class="w-full h-72 object-cover">

            <!-- Details Section -->
            <div class="p-6">
                <h2 class="text-4xl font-bold text-gray-800 mb-4"><%= house.name %></h2>
                <p class="text-lg text-gray-700 mb-4"><%= house.address %></p>
                <p class="text-2xl font-semibold text-green-600 mb-4">₹<%= house.price %> per night</p>
                <p class="text-lg text-gray-600 mb-6">
                    Nestled in a beautiful location, <%= house.name %> offers the perfect getaway with serene views and top-notch amenities.
                </p>

                <!-- Action Buttons -->
                <div class="flex gap-4">
                    <a href="/" 
                       class="flex-1 px-4 py-2 bg-gray-600 text-white rounded-lg shadow-lg hover:bg-gray-500 transition duration-300 text-center">
                        Back to Home
                    </a>
                    <a href="/house/<%= house.id %>/book" 
                       class="flex-1 px-4 py-2 bg-blue-600 text-white rounded-lg shadow-lg hover:bg-blue-500 transition duration-300 text-center">
                        Book Now
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white text-center py-4 mt-8">
        <p class="text-sm">© 2024 RentNow - All Rights Reserved</p>
    </footer>

</body>
</html>
