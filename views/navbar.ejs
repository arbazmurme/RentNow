<nav class="bg-white shadow-md fixed w-full z-10 top-0 left-0">
    <div class="container mx-auto flex items-center justify-between py-4 px-6">
        <!-- Logo -->
        <div>
            <a href="/" class="text-2xl font-bold text-gray-800 hover:text-gray-600 transition duration-300">
                RentNow
            </a>
        </div>

        <!-- Hamburger Menu Button -->
        <button id="menu-btn" class="block md:hidden focus:outline-none">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-800" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
            </svg>
        </button>

        <!-- Links -->
        <div id="menu" class="hidden md:flex space-x-6">
            <a href="/" class="<%= active === 'home' ? 'text-blue-600 font-bold' : 'text-gray-700 text-lg font-medium hover:text-blue-600 transition duration-300' %>">
                Home
            </a>
            <a href="/host/add-house" class="<%= active === 'add' ? 'text-blue-600 font-bold' : 'text-gray-700 text-lg font-medium hover:text-blue-600 transition duration-300' %>">
                Add House
            </a>
            <a href="/about" class="<%= active === 'about' ? 'text-blue-600 font-bold' : 'text-gray-700 text-lg font-medium hover:text-blue-600 transition duration-300' %>">
                About
            </a>
            <a href="/contact" class="<%= active === 'contact' ? 'text-blue-600 font-bold' : 'text-gray-700 text-lg font-medium hover:text-blue-600 transition duration-300' %>">
                Contact
            </a>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden md:hidden bg-white shadow-md">
        <a href="/" class="block py-2 px-4 <%= active === 'home' ? 'text-blue-600 font-bold' : 'text-gray-700 text-lg font-medium hover:text-blue-600 transition duration-300' %>">
            Home
        </a>
        <a href="/host/add-house" class="block py-2 px-4 <%= active === 'add' ? 'text-blue-600 font-bold' : 'text-gray-700 text-lg font-medium hover:text-blue-600 transition duration-300' %>">
            Add House
        </a>
        <a href="/about" class="block py-2 px-4 <%= active === 'about' ? 'text-blue-600 font-bold' : 'text-gray-700 text-lg font-medium hover:text-blue-600 transition duration-300' %>">
            About
        </a>
        <a href="/contact" class="block py-2 px-4 <%= active === 'contact' ? 'text-blue-600 font-bold' : 'text-gray-700 text-lg font-medium hover:text-blue-600 transition duration-300' %>">
            Contact
        </a>
    </div>
</nav>

<script>
    const menuBtn = document.getElementById('menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');

    menuBtn.addEventListener('click', () => {
        mobileMenu.classList.toggle('hidden');
    });
</script>
